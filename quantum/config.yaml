server:
  cacheDir: /tmp
  database: /database/database.db
  sources:
    - path: "/data"
      defaultEnabled: true
      createUserDir: true

auth:
  signup: false
  methods:
    noauth: false
    proxy:
      enabled: true
      header: "Remote-User"
      createUser: true

userDefaults:
  darkMode: true