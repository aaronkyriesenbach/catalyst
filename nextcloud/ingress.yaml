apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`nextcloud.lab53.net`)
      kind: Rule
      services:
        - name: nextcloud
          port: 8080
  tls:
    certResolver: letsencrypt