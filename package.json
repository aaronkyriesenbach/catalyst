{
  "name": "catalyst",
  "private": true,
  "type": "module",
  "scripts": {
    "render": "bun run scripts/render.ts",
    "sync-external-certs": "bun run scripts/sync-external-certs.ts",
    "apply-cluster": "bun run scripts/apply-cluster.ts",
    "install-argo": "helm upgrade --install -n argocd --create-namespace -f ./cluster/argocd-values.yaml argocd argocd/argo-cd"
  },
  "dependencies": {
    "@kubernetes-models/argo-cd": "^2.7.2",
    "@kubernetes-models/cert-manager": "^4.4.4",
    "@kubernetes-models/gateway-api": "^0.7.4",
    "@kubernetes-models/traefik": "^0.2.4",
    "kubernetes-models": "^4.5.1",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.11"
  }
}
