table:
  name: user_book_statuses
  schema: public
array_relationships:
  - name: user_books
    using:
      foreign_key_constraint_on:
        column: status_id
        table:
          name: user_books
          schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - description
        - id
        - slug
        - status
      filter: {}
      allow_aggregations: true
  - role: owner
    permission:
      columns:
        - description
        - id
        - slug
        - status
      filter: {}
      allow_aggregations: true
