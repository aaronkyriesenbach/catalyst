table:
  name: user_statuses
  schema: public
array_relationships:
  - name: users
    using:
      manual_configuration:
        column_mapping:
          id: status_id
        insertion_order: null
        remote_table:
          name: users
          schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - id
        - status
      filter: {}
  - role: owner
    permission:
      columns:
        - id
        - status
      filter: {}
