table:
  name: tag_categories
  schema: public
array_relationships:
  - name: tags
    using:
      foreign_key_constraint_on:
        column: tag_category_id
        table:
          name: tags
          schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - id
        - category
        - slug
        - created_at
      filter: {}
      limit: 1000
  - role: owner
    permission:
      columns:
        - id
        - created_at
        - category
        - slug
      filter: {}
      limit: 1000
