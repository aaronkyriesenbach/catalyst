table:
  name: taggable_counts
  schema: public
object_relationships:
  - name: book
    using:
      manual_configuration:
        column_mapping:
          taggable_id: id
          taggable_type: object_type
        insertion_order: null
        remote_table:
          name: books
          schema: public
  - name: tag
    using:
      manual_configuration:
        column_mapping:
          tag_id: id
        insertion_order: null
        remote_table:
          name: tags
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - taggable_id
        - hardcover_tagged
        - taggable_type
        - spoiler_ratio
        - count
        - tag_id
        - created_at
        - updated_at
      filter: {}
      limit: 998
    comment: ""
