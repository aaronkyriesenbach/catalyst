table:
  name: flag_statuses
  schema: public
array_relationships:
  - name: user_flags
    using:
      foreign_key_constraint_on:
        column: flag_status_id
        table:
          name: user_flags
          schema: public
select_permissions:
  - role: guest
    permission:
      columns:
        - id
        - status
      filter: {}
  - role: owner
    permission:
      columns:
        - id
        - status
      filter: {}
